apiVersion: v1
kind: Service
metadata:
  name: {{ include "ranking-service.fullname" . }}
  labels:
    {{- include "ranking-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ranking-service.selectorLabels" . | nindent 4 }}
